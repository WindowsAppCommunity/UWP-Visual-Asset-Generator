<UserControl
    x:Class="UWP_Visual_Asset_Generator.UserControls.WelcomeUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Visual_Asset_Generator.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:Windows10version1809="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 7)"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" x:Name="ColumnOne"/>
            <ColumnDefinition Width="*" MinWidth="420" MaxWidth="600" x:Name="ColumnTwo"/>
            <ColumnDefinition Width="*" x:Name="ColumnThree"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.ColumnSpan="3" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0.7"/>

        <Grid Grid.Column="1" Padding="24">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="1" x:Name="contentgoeshere" Background="{ThemeResource SystemControlAcrylicElementBrush}" 
                        BorderThickness="1" CornerRadius="4" BorderBrush="Silver">

                <Grid Padding="8">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <controls:DropShadowPanel BlurRadius="8"
                       Margin="0,0,12,0" Grid.Column="0"
                                          ShadowOpacity="0.7"
                                          OffsetX="2"
                                          OffsetY="2"
                                          Color="Gray">
                        <Image Source="/Assets/SmallTile.scale-200.png" />
                    </controls:DropShadowPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Welcome to" TextWrapping="WrapWholeWords" 
                                   FontWeight="Bold" Margin="0,0,0,4" />

                        <TextBlock Text="{x:Bind mainViewModel.Settings.AppName, Mode=OneWay}" Style="{ThemeResource SubheaderTextBlockStyle}"
                               VerticalAlignment="Center" TextWrapping="WrapWholeWords" HorizontalTextAlignment="Justify" />
                    </StackPanel>

                </Grid>

                <StackPanel Visibility="{Binding Settings.ShowWelcomePage, Mode=OneWay}" Padding="8">

                    <TextBlock Text="Key Features:" TextWrapping="WrapWholeWords" 
                               FontWeight="Bold" Margin="0,0,0,8"  />

                    <TextBlock Text="- ." TextWrapping="WrapWholeWords" />
                                        
                </StackPanel>

                <StackPanel Visibility="{Binding Settings.ShowUpdatePage, Mode=OneWay}" Padding="8">

                    <TextBlock Text="App Updated" FontWeight="Bold" />
                    <TextBlock Text="{Binding Settings.AppVersion, Mode=OneWay}" Margin="0,0,0,8"/>
                    
                    <TextBlock Text="- ."  TextWrapping="WrapWholeWords"/>
                </StackPanel>

                <Button x:Name="btn_Continue" HorizontalAlignment="Stretch" BorderThickness="1" Windows10version1809:CornerRadius="0,0,2,2" Margin="0,8,0,0" HorizontalContentAlignment="Stretch"
                    Background="{ThemeResource SystemControlAccentAcrylicElementAccentMediumHighBrush}" 
                        Style="{ThemeResource ButtonRevealStyle}"
                        Click="Btn_Continue_Click"
                        Padding="0">
                    <ContentControl HorizontalContentAlignment="Stretch" Padding="0" Margin="0">
                        <Grid  Padding="12">
                            <TextBlock Text="Continue" HorizontalAlignment="Center" 
                                    Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        </Grid>
                    </ContentControl>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
